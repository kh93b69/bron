# Спецификация фичи: [НАЗВАНИЕ]

> Копия этого шаблона — на каждую новую фичу, до начала реализации. Файл хранится как `features/YYYY-MM-DD_short-slug.md` или внутри PR-описания.

## Описание
- **Что делает**: коротко одним абзацем
- **Для кого**: player / club_admin / owner / super_admin
- **Зачем**: какую боль закрывает, какой метрикой будет мериться успех

## User Stories
- Как [роль], я хочу [действие], чтобы [результат]
- (минимум 3 истории: happy path + 2 edge-кейса)

## Модель данных
- Какие таблицы добавляются/меняются
- Новые колонки с типами (`uuid`, `text`, `jsonb`, `timestamptz`, `integer`, `boolean`)
- FK-связи и `ON DELETE` поведение
- Обязательные индексы
- RLS-политики (SELECT/INSERT/UPDATE/DELETE) для каждой таблицы

## API
Формат на каждый эндпоинт:
```
METHOD /path
  body: { ... }
  → 200 { ... } | 4xx (condition) | 5xx
  auth: public | authenticated | role:club_admin | role:super_admin
```

## Экраны и компоненты
- Путь (`/admin/...` или `/c/:slug/...`)
- Основные UI-элементы
- Состояния: loading / empty / success / error
- Поведение при потере сети (если применимо для PWA)

## Бизнес-логика
- Валидации (zod/yup-схема словами)
- Правила перехода статусов
- Формулы (цена, скидки, рейтинг)
- Фичи-флаги / гейтинг по тарифу

## Крайние случаи
- Что делать при конкурентном доступе (race condition)
- Поведение при недоступности внешнего сервиса (SMS, платёжка)
- Таймауты, retry, idempotency
- Дубликаты данных
- Граничные значения (пустой список, 10 000 записей, 0, -1)

## Уведомления и audit
- Какие события → в `notifications_log`
- Какие действия → в `audit_log`

## Безопасность
- Rate-limits
- Санитизация входа
- Кто может выполнять (role + проверка `club_members`)

## Приоритет / Зависимости
- Приоритет: P0 / P1 / P2
- Зависит от модулей: [список]
- Блокирует модули: [список]
- Ориентир по времени: X часов / дней

## Чеклист готовности к merge
- [ ] Миграция применена локально и на staging
- [ ] RLS-политики покрывают SELECT/INSERT/UPDATE/DELETE
- [ ] API возвращает единый формат ошибок
- [ ] Состояния loading/empty/error реализованы
- [ ] Тесты: бизнес-логика (unit) + RLS (integration) + e2e на критический путь
- [ ] i18n: все пользовательские строки через `t()`
- [ ] `qa-reviewer` прошёлся по диффу
